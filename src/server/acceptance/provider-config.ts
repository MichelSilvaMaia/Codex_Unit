import {z} from "zod";
const optional=z.string().trim().min(1).optional();
const schema=z.object({OTP_PROVIDER_MODE:z.enum(["development","production"]).default("development"),OTP_PROVIDER_TIMEOUT_MS:z.coerce.number().int().min(1000).max(30000).default(8000),ZENVIA_API_TOKEN:optional,ZENVIA_API_SECRET:optional,ZENVIA_WHATSAPP_FROM:optional,ZENVIA_WHATSAPP_OTP_TEMPLATE_ID:optional,ZENVIA_WHATSAPP_TEMPLATE_LOCALE:z.string().default("pt_BR"),ZENVIA_SMS_FROM:optional,ZENVIA_WEBHOOK_TOKEN:optional,RESEND_API_KEY:optional,RESEND_FROM_EMAIL:optional,RESEND_FROM_NAME:z.string().default("Codex Unit"),RESEND_WEBHOOK_SECRET:optional});
export type OtpProviderConfig=z.infer<typeof schema>;
export function getOtpProviderReadiness(config:OtpProviderConfig=schema.parse(process.env)){return[{channel:"WHATSAPP" as const,configured:Boolean(config.ZENVIA_API_TOKEN&&config.ZENVIA_WHATSAPP_FROM&&config.ZENVIA_WHATSAPP_OTP_TEMPLATE_ID)},{channel:"SMS" as const,configured:Boolean(config.ZENVIA_API_TOKEN&&config.ZENVIA_SMS_FROM)},{channel:"EMAIL" as const,configured:Boolean(config.RESEND_API_KEY&&config.RESEND_FROM_EMAIL)}];}
export function getOtpProviderConfig(source:Record<string,string|undefined>=process.env):OtpProviderConfig{const config=schema.parse(source);if((source.NODE_ENV==="production"||config.OTP_PROVIDER_MODE==="production")&&!getOtpProviderReadiness(config).some(item=>item.configured))throw new Error("OTP production mode requires at least one real provider");return config;}
