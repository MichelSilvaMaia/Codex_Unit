import {describe,expect,it} from "vitest";import {assertMaintenanceTransition} from "@/server/maintenance/maintenance-state-machine";import {deriveDisposition} from "@/server/returns/return-service";
describe("maintenance state machine",()=>{it.each([["OPEN","DIAGNOSING"],["DIAGNOSING","IN_PROGRESS"],["IN_PROGRESS","WAITING"],["WAITING","IN_PROGRESS"],["IN_PROGRESS","COMPLETED"],["COMPLETED","RELEASED"]] as const)("permite %s → %s",(from,to)=>expect(()=>assertMaintenanceTransition(from,to)).not.toThrow());it.each([["RELEASED","IN_PROGRESS"],["OPEN","RELEASED"],["COMPLETED","IN_PROGRESS"]] as const)("nega %s → %s",(from,to)=>expect(()=>assertMaintenanceTransition(from,to)).toThrow());it("mantém a política de devolução irregular",()=>{expect(deriveDisposition("DAMAGED")).toBe("MAINTENANCE");expect(deriveDisposition("DIRTY")).toBe("MAINTENANCE");expect(deriveDisposition("UNUSABLE")).toBe("UNAVAILABLE");expect(deriveDisposition("GOOD")).toBe("AVAILABLE")});});
